# Shared packages

`config` is active and owns environment identification. Future extraction can add `ui`, `types`, `database`, `auth` and `validation`; until then, existing root folders remain authoritative to avoid an unsafe bulk move.
