"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { ArrowLeft, LoaderCircle, LockKeyhole, Mail } from "lucide-react";
import { getSupabase } from "@/lib/supabase/client";
import { SetupBanner } from "@/components/shared/setup-banner";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const c=getSupabase();if(!c){setMessage("请先配置 Supabase 环境变量。");return;}setLoading(true);const {error}=await c.auth.signInWithPassword({email,password});setMessage(error?error.message:"登录成功，正在返回系统…");setLoading(false);if(!error)setTimeout(()=>location.assign("/"),600);}return <main className="portal-page" style={{display:"grid",placeItems:"center"}}><section className="form-card" style={{width:"min(430px,100%)"}}><Link href="/" className="muted" style={{display:"flex",gap:6,alignItems:"center",fontSize:11}}><ArrowLeft size={14}/>返回端口选择</Link><div style={{textAlign:"center",margin:"30px 0 25px"}}><span className="brand-mark" style={{margin:"auto"}}>N</span><p className="eyebrow" style={{marginTop:16}}>SECURE ACCESS</p><h1 style={{fontSize:27,margin:"8px 0"}}>登录 NEXORA</h1><p className="muted" style={{fontSize:12}}>员工与顾客使用同一个安全登录入口</p></div><SetupBanner/><form onSubmit={submit}><div className="field"><label><Mail size={13}/> 邮箱</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></div><div className="field" style={{marginTop:14}}><label><LockKeyhole size={13}/> 密码</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></div>{message&&<div className="notice" style={{marginTop:14}}>{message}</div>}<button className="button primary" style={{width:"100%",marginTop:18}} disabled={loading}>{loading&&<LoaderCircle size={15}/>}登录</button></form><p className="muted" style={{fontSize:10,textAlign:"center",marginTop:17}}>员工账号由 Owner 创建；顾客注册将在正式营业前开放。</p></section></main>}
