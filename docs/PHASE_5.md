# 第5阶段：批量快速入库

- 新增 `/inbound/batch` Excel 式连续录入表格。
- 支持直接粘贴 Tab、逗号或分号分隔的“款号、颜色、数量”三列数据。
- 颜色可使用中文名、英文名或SKU颜色代码匹配。
- Enter 自动跳转到下一个控件，支持新增、删除及复制上一行款号。
- 错误行红色提示，正确行绿色提示；相同款号与颜色默认合并。
- 整批数据调用同一个数据库事务函数，任一行失败时全部回滚。
