# 第8阶段：商品图片上传与管理

- 商品详情支持手机后置摄像头直接拍照及一次选择多张图片。
- 上传前在浏览器自动缩放至最长边2000像素并压缩为WebP；仍保留10MB源文件限制。
- 图片保存到 `products/{product_id}/{timestamp}-{uuid}`，数据库只保存路径与公开URL。
- 支持设置主图、前移、后移、预览和确认删除。
- 主图与排序通过受控事务函数更新并写入审计日志，避免多张主图或排序中断。
- 上传数据库记录失败时自动清理已上传文件，避免孤立对象。
