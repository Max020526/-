# 第14阶段：全量验收与部署

## 自动化验收

- `npm run lint`：通过，0 错误、0 警告。
- `npm run typecheck`：通过。
- `npm run build`：Sites/Vinext 生产构建通过。
- `npm run build:netlify`：Netlify/Next.js 生产构建通过。
- `npm test`：页面、数据库基础、解析、权限、入库校验、CSV 与错误映射测试通过。
- `npm audit --omit=dev`：运行并记录结果。

## 业务验收清单

- [x] 员工登录、停用检查与角色路由。
- [x] 单款多颜色快速入库、SKU 自动创建、库存原子增加。
- [x] 批量粘贴、重复合并、错误高亮、整批回滚。
- [x] 今日入库、明细和管理员取消反向流水。
- [x] 商品资料、价格、描述、分类、颜色尺码和状态。
- [x] 手机拍照、多图压缩、主图、排序和删除。
- [x] 库存查询、低库存、流水和带原因的盘点修正。
- [x] 仪表盘、颜色、分类、供应商、员工和审计日志。
- [x] UTF-8 BOM CSV 导出与公式注入保护。
- [x] 手机底部导航、PWA 安装与离线提示。
- [x] Supabase RLS、Storage 策略、受控 RPC 和显式 API grants。

## 上线前人工检查

1. 在部署平台配置三个公开环境变量和仅服务端可见的 `SUPABASE_SECRET_KEY`。
2. 使用一个管理员和一个员工账号分别登录，验证价格、上架、设置页的权限差异。
3. 用真实手机完成拍照、快速入库、多图上传和 PWA 安装。
4. 在 Supabase Auth 安全设置中启用泄露密码保护。
5. 使用两台设备并发录入相同款号与颜色，确认库存和入库编号正确。
