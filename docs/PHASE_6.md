# 第6阶段：今日入库、明细与安全取消

- 新增 `/inbound/today`，按当天显示入库单号、员工、款号数、颜色数、件数和状态。
- 新增 `/inbound/[id]`，显示每个SKU的入库前库存、入库数量和入库后库存。
- 管理员可填写原因取消已确认入库；员工只能查看自己的记录。
- 取消操作锁定相关库存行，库存不足时整次拒绝，防止产生负库存。
- 取消不会删除单据，会将状态改为 `cancelled`，生成 `ADJUSTMENT_OUT` 反向流水与审计日志。
