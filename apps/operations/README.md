# Operations application boundary

Current compatible routes live under `/warehouse`, `/inbound` and `/inventory` in the root Next.js application. The extracted application must keep warehouse and category scope enforcement server-side.
