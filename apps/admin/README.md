# Admin application boundary

Current compatible routes live under `/admin` in the root Next.js application. Future extraction must preserve auth, RBAC and audit behavior before this directory becomes a Netlify base directory.
